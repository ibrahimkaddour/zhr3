<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record model="ir.actions.server" id="action_change_policy">
        <field name="name">Change Policy</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="product.model_product_template"/>
        <field name="binding_model_id" ref="product.model_product_template"/>
        <field name="state">code</field>
        <field name="code">action = records.change_policy()</field>
<!--        <field name="confirm">Are you sure about the save process ??</field>-->
    </record>
</odoo>